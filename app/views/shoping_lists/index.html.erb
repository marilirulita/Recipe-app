<h1 class="center">Shoping list</h1>
<div class="fle-box">
    <h3>
      Amount of food items to buy: <%= @total_amount%>
    </h3>
    <h3>
      Total value of food needed: $<%= @total_value%>
    </h3>
</div>
<table>
  <tr><th>Food</th><th>quantity
    <span>
  <%= link_to '<', user_shoping_lists_path(sort: true, row:'price', order: "asc")%>
  <%= link_to '>', user_shoping_lists_path(sort: true, row:'price', order: "des") %>
  </span>
  </th><th>price
    <span>
  <%= link_to '<', user_shoping_lists_path(sort: true, row:'price', order: "asc")%>
  <%= link_to '>', user_shoping_lists_path(sort: true, row:'price', order: "des") %>
  </span></th></tr>
  <% @shoping_lists.each do |food| %>
  <tr>
    <td><%= food.name%></td>
    <td><%= food.quantity%></td>
    <td>$<%= food.price%></td>
  </tr>
  <% end %>

</table>

